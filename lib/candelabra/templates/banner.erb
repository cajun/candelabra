    usage: candelabra [command] [data]
    
    Base Commands:
      install
      start
      stop
      restart
    
    Controlling Pianobar
      n(ext)
      p(ause)
      co(continue)
      ch(change_station) # of station
      t(ired)
      u(pcomming)
      q(ick_mix)
    
